# MR Mobile Zone — Frontend (main)

Next.js marketing site + admin CRM. Talks to the API on the `backend` branch.

```bash
cd frontend
npm install
cp .env.example .env.local   # set NEXT_PUBLIC_API_URL
npm run dev                  # http://localhost:3000
```

API: see branch `backend` (`backend/` folder).
